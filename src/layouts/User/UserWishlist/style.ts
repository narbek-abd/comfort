import styled from "styled-components";
import { Alert } from "components/Alert/style";

export const UserWishlist = styled.section`
	margin-top: 60px;
	${Alert} {
		margin-top: 40px;
	}
`;

export const Wishlist = styled.div``;
