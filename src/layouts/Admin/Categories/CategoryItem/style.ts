import styled from "styled-components";
import { Button } from "../../../../globalStyle";

export const CategoryItem = styled.tr`


`

export const Actions = styled.div`
	${Button} {
		margin-right: 10px;
		a {
			color: #fff;
		}
	}
`;