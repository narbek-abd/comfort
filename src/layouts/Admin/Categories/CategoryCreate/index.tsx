import React from 'react';
import * as S from "./style";

interface CategoryCreateProps {
  children?: React.ReactNode;
}

const CategoryCreate = ({children} : CategoryCreateProps) => {
  return (
    <S.Create>
    <form action="#">ss</form>
    </S.Create>
  );
};

export default CategoryCreate;